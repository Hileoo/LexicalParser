#include <stdio.h>
int main()
{
    int height, height_1 = 20;
    int width = 10;
    char ch = 'c';
    
    /* There is the if statement */
    if(height >= width) {
        int area = height * width;
        //If the area is more than 100
        if(area > 100) {
            int area += 100;
            float percentage = 0.2312;
            printf("The total area is " + total_area);
            if((@$ <= 1 && area <1000)) {
                height++;
            }
        }
        else {
            printf("It is a small area: " + area);
        }
        
    }
    return 1;
}
